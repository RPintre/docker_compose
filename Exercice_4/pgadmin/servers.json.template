{
  "Servers": {
    "1": {
      "Name": "PostgreSQL",
      "Group": "Servers",
      "Host": "${POSTGRES_HOST}",
      "Port": ${POSTGRES_PORT},
      "Username": "${POSTGRES_USER}",
      "SSLMode": "prefer",
      "MaintenanceDB":"postgres"
    }
  }
}
